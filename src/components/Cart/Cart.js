import React from 'react';
import Breaks from '../Breaks/Breaks';
import Profile from '../Profile/Profile';

const Cart = () => {
    return (
        <div>
            <Profile></Profile>
            <Breaks></Breaks>
        </div>
    );
};

export default Cart;